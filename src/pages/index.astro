---
import Layout from '../layouts/Layout.astro';
import Landing from '../components/Landing.astro';
import { getCopy } from '../i18n/copy';

const locale = 'en';
const c = getCopy(locale);
---

<Layout locale={locale} c={c}>
  <Landing locale={locale} c={c} />
</Layout>
